services:
  - type: web
    name: creators-stats
    runtime: node
    plan: free
    buildCommand: "echo no build step"
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: "20"
